
### In Java

```java
condition ? then : else
```

### In Kotlin

In Kotlin, we don't have that ternary operator.
Instead, use one line if statement:

```kotlin
val a = 1
val b = 2

println(if (a > b) a else b) // Returns a value: 2
```