
Instead of:

```kotlin
fun sum(x: Int, y: Int): Int {
    return x + y
}
```

You can write:

```kotlin
fun sum(x: Int, y: Int) = x + y
```

---

> However, don't forget that your code should be understood by other developer quickly.