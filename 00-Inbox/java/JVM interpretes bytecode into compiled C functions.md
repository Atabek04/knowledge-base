### You can't execute Bytecode as machine code

JVM interpreter is written in C

When interpreter sees bytecode, it maps bytecode instructions to native machine code (which is already compiled C functions) 

Read more about:
- [[Java bytecode isn't Machine code]]