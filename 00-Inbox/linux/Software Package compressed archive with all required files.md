
App usually have dependencies.

You have dependency of some JS tools, it requires `Node.js`
So that dependencies needs to be installed first
Because they're not packaged into archive

---

Installing software on Linux is complex, because files are split across different folders: `bin`, `lib`

That's why uninstalling might also be difficult, in order to remove all files