
With `apt-get` you can achieve the same,
if you use additional command options

> search command NOT available

---

`apt` is more user-friendly
has progress bar
fewer commands that `apt-get`
but sufficient command options in a more organized way