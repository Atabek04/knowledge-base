
Repository - storage location, containing thousands of programs

Package manager - fetches the packages from these repositories

Always update the package index
before upgrading or installing new packages

```bash
sudo apt update
```

Updating package index means:
- pulling the latest changes from the APT repos
- APT package index is basically a database