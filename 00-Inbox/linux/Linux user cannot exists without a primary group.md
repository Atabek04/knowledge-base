
Every user must have exactly one primary group.
It's mandatory in Linux user system.

---
#### What you can do

Change primary group to different one.
Leave secondary groups - those are optional
But you always need one primary group.

---

By default, when you create a user, by default the primary group will be created with the same username.