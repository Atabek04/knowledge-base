> Type Inference is only used for local variables

So, from Java 10, it is no longer necessary to specify the type of the variable when declared.
Type can be inferred from the value assigned to the variable.

---

```
String myVar = "A string!";
```

```
var myVar = "A string!";
```
